Making a Type Card Component.  A type card component is a self-contained UI element used to display a persons' information. Typically a name, role, bio. Common on team pages, user profiles and contact directories.
It displays a name, title, short bio and image using HTML / CSS.
To be UPDATED